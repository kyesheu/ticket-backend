# v1.3 架构设计

状态：已完成

## 访问策略

ITicketAccessPolicy 集中提供列表范围注入和单工单访问断言。Controller 不拼接范围，业务 Service 不遍历角色，Mapper 只接收服务端生成的安全范围。

可访问工单 = 有效角色的部门范围并集 ∪ 本人创建 ∪ 当前指派给本人。管理员跳过部门过滤。

## 覆盖入口

工单列表、详情、评论、操作日志、评价及状态操作先执行对象访问控制，再执行各自身份和状态规则。拒绝结果不得泄露工单是否存在。

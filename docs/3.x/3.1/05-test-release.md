# v3.1 测试与发布

状态：完成

## 必测

- 合法候选、空候选、停用分类、停用用户和跨部门用户。
- 分类、优先级、处理人不在候选集；置信度越界和伪造来源。
- 无证据、提示词注入、模型超时、非法 JSON 和超长输出。
- 采纳、修改后采纳、拒绝、重复确认和并发确认。
- 工单 updateTime、状态、权限或候选人在确认前变化。
- AI 故障时原工单主流程不受影响。

## 发布门禁

- [ ] Python 和 Java 单元/契约测试通过。
- [ ] sql/ticket-v3.1.sql 在 v3.0 数据库执行成功。
- [ ] 全量 Maven 测试、编译和 Python compileall 通过。
- [ ] 完整依赖端到端 smoke 和 v1.0–v3.0 回归通过。
- [ ] 日志、数据库和响应不包含正文、提示词或凭据泄露。

## 阶段记录

- 阶段 49：HTTP 契约、SQL 和 Java/Python 骨架已完成。
- 阶段 50：分类与优先级建议、候选白名单和来源校验已完成。
- 阶段 51：处理人候选生成、排序和跨部门权限已完成。
- 阶段 52：建议持久化、采纳/修改/拒绝和过期控制已完成。
- 阶段 53：固定评测集已补充 v3.1 triage 场景，主 smoke 已覆盖建议生成、采纳、拒绝和重复确认失败；待用户执行完整回归和端到端 smoke。

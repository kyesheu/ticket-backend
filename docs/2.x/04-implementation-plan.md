# 04 — 实施计划

> 2.x | 2026-07-04 | 状态: ✅ 已完成

## v2.0 阶段二十三：流程数据模型与定义骨架 ✅

- 先列版本唯一性、发布后不可变、流程图合法性、绑定回退的合法/非法/边界测试用例
- 新增 `sql/ticket-v2.0.sql`：5 张流程表、分类绑定字段、内置标准流程和菜单权限
- 只建立定义、节点、连线、实例、任务所需 Domain、Enum、Mapper 及 XML 骨架
- 所有 POJO 实现 `Serializable` 并声明 `serialVersionUID`
- 不修改 `ruoyi-common`、`ruoyi-framework`、`ruoyi-system`、`ruoyi-quartz` 等基础模块
- 验证：SQL 人工审查、目标 Mapper 测试、`mvn test`、`mvn clean compile`

## v2.0 阶段二十四：流程定义管理与发布校验 ✅

- 先列草稿 CRUD、复制版本、发布校验、重复发布和并发发布测试用例
- 实现流程定义查询、草稿保存、版本复制、发布和分类绑定接口
- 发布校验一个 START、至少一个 END、节点可达、人工节点处理人完整、唯一默认分支
- 已发布定义、节点和连线不可修改或删除；分类只能绑定存在已发布版本的 `workflow_key`
- 验证：Service Mockito 单元测试、Controller 权限测试、定义管理接口 smoke

## v2.0 阶段二十五：流程引擎启动与条件路由 ✅

- 先列标准流程、版本锁定、三类条件、默认分支、无匹配分支和处理人无法解析测试用例
- 建立 `ITicketWorkflowEngine` 及实现，创建工单时解析分类绑定并启动实例
- 实现五种处理人规则解析和任务快照，包含标准流程所需的工单指派人和创建人
- 条件字段与运算符使用枚举白名单，不解析任意表达式
- 验证：引擎接口单元测试、创建工单集成测试、实例和首任务接口 smoke

## v2.0 阶段二十六：任务处理、退回、取消与终止 ✅

- 先列合法处理、越权、重复提交、并发提交、退回边界、取消和终止测试用例
- 实现待办列表、任务详情、完成、退回、取消和管理员终止接口
- 单事务更新任务、实例、工单状态、操作日志和通知；条件更新保证一个任务只完成一次
- 退回只到已完成的直接上一人工节点，不删除或覆盖历史任务
- 验证：Service Mockito 单元测试、并发与幂等测试、任务动作接口 smoke

## v2.0 阶段二十七：旧流程兼容与跨模块回归 ✅

- 先列历史无实例工单、标准流程旧接口、SLA、通知、评价和访问控制回归用例
- 历史工单保留原状态机；新标准流程工单由兼容适配调用流程引擎
- 业务状态映射保持现有列表、SLA、通知和统计语义
- 所有流程实例和任务入口先执行 `ITicketAccessPolicy`，再校验任务处理权限
- 验证：v1.0–v1.3 全量单元测试与回归 smoke

## v2.0 阶段二十八：联调收尾 ✅

- 扩展 `scripts/ticket/v3.x/smoke-test.ps1`，覆盖定义发布、版本锁定、条件路由、五种处理人、退回和并发幂等
- 执行 `mvn test` → `mvn clean compile` → 启动后端 → 完整 smoke
- 检查 Swagger、增量 SQL 可重复部署失败保护、权限和敏感错误信息
- 更新当前目录 `05-test-release.md` 实测结果
- 未获得全部测试结果前不得将 v2.0 标记完成

## v2.1 阶段二十九：增量数据库与基础模型 ✅

- 先列字段唯一性、类型参数、值快照、流程条件字段的合法/非法/边界测试用例
- 新增 `sql/ticket-v2.1.sql`：3 张表、流程条件 key、菜单权限
- 只建立定义、选项、值所需 Domain、Enum、Mapper/XML 骨架
- 验证：Docker SQL 执行审查、模型测试、`mvn test`、`mvn clean compile`

## v2.1 阶段三十：字段定义与选项管理 ✅

- 实现分类字段列表、详情、新增、修改、启停接口
- 校验 key 唯一、类型参数、选项唯一和字段不可变属性
- 定义与选项在同一事务保存，不提供物理删除
- 验证：Service Mockito 测试、权限测试、管理接口 smoke

## v2.1 阶段三十一：创建校验、值快照与详情 ✅

- 扩展创建 DTO，建立 `ITicketCustomFieldService` 深模块
- 覆盖必填、类型、长度、范围、未知/重复字段、选项和停用字段测试
- 工单、字段值和流程实例同事务保存；详情返回字段快照
- 验证：Service 测试、事务回滚测试、创建与详情接口 smoke

## v2.1 阶段三十二：自定义字段流程条件 ✅

- 扩展流程条件 `CUSTOM_FIELD + condition_key`
- 发布时校验 key 格式，运行时读取规范化值执行 `EQ / IN`
- 缺失值或类型不兼容不命中，禁止脚本和 SQL 表达式
- 验证：流程引擎条件测试、版本兼容测试、路由接口 smoke

## v2.1 阶段三十三：联调收尾 ✅

- 扩展主 smoke 覆盖七种字段、快照、禁用字段、非法值和自定义字段路由
- 执行 `mvn test` → `mvn clean compile` → 启动后端 → 完整 smoke
- 检查 Swagger、数据库一致性、权限和敏感错误信息
- 更新当前目录 `05-test-release.md` 实测结果，全部通过后标记 v2.1 完成

## v2.2 阶段三十四：增量数据库与基础模型 ✅

- 先列业务归属、元数据快照、大小边界、重复绑定、跨工单归属和软删除测试用例
- 新增 `sql/ticket-v2.2.sql`：附件元数据表和菜单权限
- 只建立附件 Domain、Enum、Mapper/XML 骨架，不保存文件二进制
- 不修改 `ruoyi-common`、`ruoyi-framework`、`ruoyi-system` 等基础模块
- 验证：SQL 人工审查、模型测试、`mvn test`、`mvn clean compile`

## v2.2 阶段三十五：附件上传、查询与下载 ✅

- 复用 RuoYi 文件存储配置，实现附件上传、列表和受控下载
- 校验文件非空、文件名、扩展名、MIME、单文件大小和单工单数量
- 临时上传记录当前用户；工单关联入口先执行 `ITicketAccessPolicy`，下载不暴露服务器绝对路径
- 验证：Service Mockito 测试、Controller 权限测试、上传下载接口 smoke

## v2.2 阶段三十六：创建与评论附件绑定 ✅

- 创建工单和添加评论时支持绑定已上传的临时附件
- 校验上传人、业务类型、目标工单、单工单附件数量和重复绑定，绑定操作与业务保存处于同一事务
- 工单正文附件和评论附件分别展示，历史附件元数据保持不变
- 验证：事务回滚测试、越权绑定测试、创建与评论接口 smoke

## v2.2 阶段三十七：附件删除与存储清理 ✅

- 上传人或管理员可逻辑删除附件，终态工单附件默认不可删除
- 数据库提交后再清理物理文件，物理清理失败保留可补偿记录和错误日志
- 不允许通过路径参数直接删除文件，防止目录穿越
- 验证：权限、重复删除、终态、文件缺失和清理失败测试

## v2.2 阶段三十八：联调收尾 ✅

- 扩展主 smoke 覆盖上传、绑定、查询、下载、删除、越权、非法类型和大小边界
- 执行 `mvn test` → `mvn clean compile` → 启动后端 → 完整 smoke
- 检查 Swagger、数据库一致性、路径安全、权限和敏感错误信息
- 更新当前目录 `05-test-release.md` 实测结果，全部通过后标记 v2.2 完成

## v2.3 阶段三十九：增量数据库与检索骨架 ✅

- 先列事件事务一致性、重复/乱序消费、权限隔离、游标和故障降级的合法/非法/边界测试用例
- 新增 `sql/ticket-v2.3.sql`：检索事件表和菜单权限
- 引入与当前 Spring Boot 版本兼容的 Elasticsearch 客户端配置，只建立当前阶段需要的事件 Domain、Enum、Mapper/XML 和检索接口骨架
- 默认配置下不强制连接 ES，保证既有业务和测试可独立运行；不修改 `ruoyi-common`、`ruoyi-framework`、`ruoyi-system`
- 验证：SQL 人工审查、配置绑定测试、模型测试、`mvn test`、`mvn clean compile`

## v2.3 阶段四十：索引投影与可靠同步 ✅

- 工单创建、修改、流转和评论变更在原事务内追加 `UPSERT` 事件
- 实现事件抢占、超时恢复、退避重试、失败补偿和按 `source_event_id` 防乱序覆盖
- 从 MySQL 聚合单工单检索投影，索引不保存附件路径、操作日志和敏感字段
- 验证：Service Mockito 测试、事务回滚测试、并发幂等测试、Testcontainers Elasticsearch 集成测试

## v2.3 阶段四十一：全文检索、过滤与权限 ✅

- 实现关键词、白名单过滤、稳定排序、`search_after` 游标和安全高亮
- ES 数据范围过滤与 `ITicketAccessPolicy` 批量复核共同保证对象级权限；不足一页时继续召回
- 拒绝客户端 DSL、非法排序、篡改游标和超大页；ES 不可用返回明确业务错误
- 验证：查询构造测试、权限测试、游标边界测试、Controller 测试和检索接口 smoke

## v2.3 阶段四十二：全量重建与运维补偿 ✅

- 管理员可触发单任务全量重建、查询进度、重试失败事件
- 使用版本化索引、数据库主键游标批处理和固定别名原子切换；重建失败不影响线上别名
- 防止并发重建，记录开始、进度、完成和失败日志，不向接口返回 ES 敏感错误
- 验证：重建状态机测试、别名切换集成测试、失败恢复和管理接口 smoke

## v2.3 阶段四十三：联调收尾 ✅

- 扩展主 smoke 覆盖同步、全文关键词、组合过滤、权限、游标、失败补偿和重建切换
- 执行 `mvn test` → `mvn clean compile` → 启动 MySQL/Redis/Elasticsearch 与后端 → 完整 smoke
- 检查 Swagger、索引 mapping、数据库一致性、权限、高亮转义和敏感错误信息
- 更新当前目录 `05-test-release.md` 实测结果，全部通过后标记 v2.3 完成


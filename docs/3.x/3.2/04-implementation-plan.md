# v3.2 实施计划

状态：开发中（阶段 54）

| 阶段 | 内容 | 验证 |
|---|---|---|
| 54 | 知识管理契约、反馈 SQL、模型和测试骨架 | 契约、权限、SQL 和编译测试 |
| 55 | 文档列表、详情、删除、重导和失败恢复 | 幂等、半成品、删除后检索和并发测试 |
| 56 | AI 反馈、采纳关联和运营指标 | 身份、唯一性、统计口径和越权测试 |
| 57 | 评测版本、结果对比、全量回归和 smoke | 固定评测集、pytest、Maven 和端到端测试 |

前端若属于本项目交付范围，本版本补齐知识管理、工单 AI 助手、分诊确认和反馈页面；若前端由独立仓库负责，只保留稳定 API 契约。

## 当前执行：阶段 54

1. 列出测试用例：契约字段、分页边界、反馈枚举、唯一键、禁止保存模型正文。
2. 更新规划文档，使 v3.2 从规划态进入阶段 54 开发态。
3. 新增 `sql/ticket-v3.2.sql` 和反馈模型、Mapper、XML 骨架。
4. 新增 Java 到 Python 的知识文档列表/详情契约骨架。
5. 新增 Python 文档管理契约模型和 HTTP 测试。
6. 执行聚焦测试、`mvn test`、`mvn clean compile`；依赖服务可用时再执行 smoke。

# v2.2 架构设计

状态：已完成

- 附件元数据归属 ticket-ticket，文件内容复用 Ticket 本地上传目录。
- 上传先生成 TEMP 记录；创建工单或评论时在原事务中绑定业务对象。
- 下载由附件 Service 校验归属、对象访问权和规范化路径后返回资源。
- 删除先提交逻辑状态，再清理物理文件；失败保留可补偿状态。
- 文件名由服务端生成，相对路径必须解析在允许的 upload 根目录内。
